/** Grid placement in 12-column units. */
export type Rect = { x: number; y: number; w: number; h: number };

export type WidgetId =
  "periodNav" | "summary" | "digest" | "calendar" | "equity" | "metrics" | "journal";
